# -------- Python image / version --------
PYTHON_IMAGE=python:3.14-slim

# Container names (Dokploy may override internally, but these are good defaults)
CLI_CONTAINER_NAME=cli
WEBDAV_CONTAINER_NAME=webdav

# -------- CLI command --------
# This will run *inside* the cli container, after dependencies are installed.
# By default it runs main.py in /app.
CLI_COMMAND=python main.py

# -------- WebDAV auth (for files from any device) --------
WEBDAV_USERNAME=myuser
WEBDAV_PASSWORD=mypassword
